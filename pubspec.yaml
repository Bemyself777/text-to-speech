name: text_to_speech
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^3.8.5  # 添加 TTS 语音合成插件

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
